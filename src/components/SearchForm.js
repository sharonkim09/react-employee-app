import React from 'react';

const SearchForm = (props) => {
    return (
        <>
            <input 
            type="text"
            className="form-control"
            placeholder="Search"
            // onChange=""
            />
            
        </>
    );
};

export default SearchForm;